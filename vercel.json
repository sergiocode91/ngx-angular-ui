{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/documentation" }
    }
  ],
  "routes": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}